import { LoadingStatus } from "@/types/Loading";

export const LOADING_STATUS = {
  IDLE: 'IDLE' as LoadingStatus,
  SUCCESS: 'SUCCESS' as LoadingStatus,
  PENDING: 'PENDING' as LoadingStatus,
  ERROR: 'ERROR' as LoadingStatus,
};